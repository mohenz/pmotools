# PMOTOOLS 플랫폼화 기획서

작성일: 2026-09-03
버전: v0.3 (초안 — 목표 아키텍처 + 테스트 보조용 결함관리 통합 + 단계별 로드맵)

## 1. 배경 및 목적

현재 PMOTOOLS는 사실상 **단일 프로젝트 전용 도구**로 운영되고 있다. 데이터 모델(`Project`, `ProjectMember`)은 한 사용자가 여러 프로젝트에 속할 수 있는 다대다(N:M) 구조를 이미 갖추고 있지만, 실제 로그인·화면 로직은 `DEFAULT_PROJECT_ID` 상수 하나에 하드코딩되어 있어 이 구조를 전혀 활용하지 못하고 있다.

이번 플랫폼화의 목적은 PMOTOOLS를 **여러 프로젝트(및 프로젝트 그룹)를 동시에 운영할 수 있는 플랫폼**으로 전환하는 것이다. 요구사항 원문 기준 핵심 목표는 다음과 같다.

- 1개 이상의 프로젝트를 동시에 운영
- 슈퍼관리자가 프로젝트를 생성하고 PM/PMO를 지정
- 프로젝트 PM이 자신의 프로젝트 정보를 관리하고, 사용할 기능(기본/추가)을 선택
- 사용자는 가입 시 프로젝트를 검색해서 가입 신청
- 슈퍼관리자가 상위-하위 프로젝트를 지정하는 2단계 계층 구조
- 하위 프로젝트는 개별적으로 WBS·요구사항관리를 하면서, 상위 프로젝트에서는 하위 전체를 모은 **통합 현황(집계/롤업 보고)**도 볼 수 있음
- 로그인 시 프로젝트를 선택하고, "기억하기"로 다음 로그인부터 자동 선택
- 기존 `defect_manage2`의 결함관리 기능과 데이터를 PMOTOOLS 내부 프로젝트 종속 모듈로 통합
- 결함관리를 테스트 수행 중 발견사항을 등록·조회·조치하는 독립적인 테스트 보조 수단으로 제공

## 2. 현재 상태 진단 (코드 기준)

| 영역 | 현재 상태 | 시사점 |
|---|---|---|
| `User` | 전역 계정 1개, 전역 `role`(SUPER_ADMIN/ADMIN/OPERATOR/MEMBER) 보유. 프로젝트에 종속되지 않음 | 계정 자체는 이미 여러 프로젝트에 걸쳐 재사용 가능한 구조 |
| `ProjectMember` | `(projectId, userId)` 유니크, **프로젝트별 role**을 별도로 가짐 | 한 사용자가 프로젝트마다 다른 역할(관리자/운영자/일반)을 가지는 구조가 **이미 존재** — 활용만 안 되고 있음 |
| `Project` | 단일 평면 엔티티. 상하위 관계·기능 On/Off 필드 없음 | 계층·기능선택은 신규 스키마 확장 필요 |
| 로그인 (`lib/server/auth.ts`) | `authorize()`가 항상 `DEFAULT_PROJECT_ID`로만 멤버십을 조회. 세션의 `projectId`도 항상 이 값 고정 | **가장 큰 변경 지점.** 실제 멤버십 기반 로그인으로 전면 재작성 필요 |
| 회원가입 (`app/signup`, `/api/auth/signup`) | `User`만 생성, 프로젝트 연결 없음 → 가입 후 관리자가 수작업으로 `ProjectMember` 추가해야 로그인 가능 | 가입 흐름에 "프로젝트 검색·선택" 단계 자체가 없음 |
| PM/PMO | `jobTitle`(자유 텍스트)이 `"PM"`/`"PMO"`인지로 판별(`lib/domain/job-access.ts`)하는 **설명적 속성**이지 프로젝트별 권한이 아님 | PM 지정은 "그 프로젝트의 `ProjectMember.role`을 관리자급으로 부여"로 구현하는 것이 기존 구조와 가장 잘 맞음 |
| 요구사항관리·WBS·이슈관리 등 | 이미 모든 도메인 함수(`lib/server/*.ts`)가 `projectId`를 명시적으로 받아 Prisma 쿼리를 필터링 | **재사용 가능한 자산.** 멀티 프로젝트 자체를 위한 데이터 격리는 이미 되어 있음 — 문제는 "어떤 projectId를 쓸지 결정하는 상위 로직"이 없다는 것 |
| 공통코드 설정 화면 | `app/settings/common-codes` → `screens/CommonCodeSettingsScreen.tsx` → API route → `lib/server/common-codes.ts` → Prisma | 신규 관리 화면들이 그대로 따라야 할 **표준 스택 패턴** |
| 결함관리 | `D:\workspace\defect_manage2`에 Vanilla JS + Express + 독립 PostgreSQL 앱으로 존재. 자체 사용자·공통코드·API·로그인 체계를 사용하며 프로젝트 구분이 없음 | 소스 폴더를 정적으로 복사하지 않고 PMOTOOLS의 Next.js·Prisma·인증·프로젝트 권한 체계에 맞는 내부 모듈로 이식해야 함 |

**결론:** 데이터 격리·권한 체크의 하부 배관(plumbing)은 이미 갖춰져 있다. 이번 작업의 본질은 (1) 로그인/세션이 "프로젝트 하나 고정"을 벗어나게 하는 것, (2) 그 위에 슈퍼관리자·PM 전용 관리 화면들을 새로 얹는 것이다.

## 3. 목표 아키텍처

### 3.1 데이터 모델 확장

```
Project
  + parentProjectId  String?  (자기참조, 상위 프로젝트)
  + 검증: parentProjectId가 있는 프로젝트는 다시 자식을 가질 수 없음 (2단계 강제, 앱 레이어에서 검증)

ProjectFeature (신규 테이블)
  projectId   String
  featureKey  String   (예: "wbs", "requirements", "meetrooms", "pmo_daily", ...)
  enabled     Boolean  @default(true)
  @@unique([projectId, featureKey])

ProjectMember.role (기존 필드 재해석, 신규 컬럼 없음)
  ADMIN     → 그 프로젝트의 PM (프로젝트 정보 수정, 기능 선택, 가입 승인 권한)
  OPERATOR  → 그 프로젝트의 PMO (관리 보조 권한, 기존 "운영자"와 동일 위계 재사용)
  MEMBER    → 일반 참여자
  isActive  → 가입 승인 대기(false) / 승인 완료(true) 상태로 재사용

User
  + lastProjectId  String?  (마지막으로 로그인한 프로젝트 — "기억하기" 서버측 저장)

Defect (신규 테이블)
  id                   String   (UUID, PMOTOOLS 내부 PK)
  projectId            String   (프로젝트별 데이터 격리)
  legacyDefectId       BigInt?  (`defect_manage2` 원본 번호 보존)
  title / testType / severity / priority / status
  stepsToReproduce / menuName / screenName / screenUrl
  screenshotPath       String?  (DB Base64 대신 Storage 경로 또는 URL)
  identification / environmentInfo
  creatorId / assigneeId       (PMOTOOLS User FK, 미매핑 원본명은 별도 보존)
  actionComment / actionDueDate / actionStart / actionEnd
  archivedAt / createdAt / updatedAt
  + @@unique([projectId, legacyDefectId])

DefectEvent (신규 테이블)
  defectId / eventType / actorId / beforeData / afterData / createdAt

DefectErrorLog (신규 테이블)
  projectId / defectId? / operation / stage / errorType / message
  errorCode / errorDetails / runtimeContext / payloadSummary / reportedById? / createdAt

DefectSequence (신규 테이블)
  projectId String @id
  value     BigInt
```

- `jobTitle`(PM/PMO 자유 텍스트)은 그대로 유지하되, 프로젝트 생성 시 슈퍼관리자가 "PM/PMO 지정"하는 행위는 **해당 사용자의 `ProjectMember.role`을 ADMIN/OPERATOR로 설정**하는 것으로 구현한다. `jobTitle`은 설명용, `ProjectMember.role`이 실질 권한이라는 기존 원칙을 유지.
- `ProjectFeature`를 별도 테이블로 둔 이유: 공통코드 테이블(`CommonCodeGroup`/`CommonCode`)과 동일한 "정규화된 설정 테이블" 패턴을 따라 감사·확장(기능별 세부옵션 추가 등)이 쉽도록 함. 켜지지 않은 `featureKey`는 "기본 OFF"로 취급.
- **기본기능 vs 추가기능**: 코드 레벨의 `FEATURE_CATALOG` 상수로 각 `featureKey`에 `tier: "base" | "addon"`을 부여. base는 모든 프로젝트에 항상 `enabled=true`로 강제(토글 UI 비활성)하고, addon만 PM이 켜고 끌 수 있게 한다. (기본기능 후보: 요구사항관리, WBS, 캘린더/회의실, 공지사항 / 추가기능 후보: PMO Daily, 이슈관리, **결함관리**, 업무일지, 위클리리포트, 주간실적, 인력변동 — 실제 분류는 3.6절에서 확정 필요)

### 3.2 권한 모델

| 레벨 | 역할 | 권한 범위 |
|---|---|---|
| 플랫폼 | `SUPER_ADMIN` (User.role) | 모든 프로젝트 생성/조회, PM·PMO 지정, 상하위 프로젝트 지정, 모든 프로젝트 데이터 접근 |
| 프로젝트 | `ProjectMember.role = ADMIN` (PM) | 자기 프로젝트 정보 수정, 기능 On/Off, 가입 승인/반려, 프로젝트 멤버 관리 |
| 프로젝트 | `ProjectMember.role = OPERATOR` (PMO) | PM 보조 — 기존 매니저 권한(`isManagerRole`)과 동일 위계 |
| 프로젝트 | `ProjectMember.role = MEMBER` | 일반 참여자, 승인된 기능 내에서 활동 |

기존 `assertManager`/`assertAdmin`/`isManagerRole`(lib/server/permissions.ts) 등은 모두 "현재 세션의 projectId 컨텍스트 안에서" 이미 이렇게 동작하고 있으므로 **그대로 재사용**한다. 신규로 필요한 것은 "플랫폼 레벨(SUPER_ADMIN, 프로젝트 컨텍스트 없이)" 게이트 하나뿐이다 (`requireSuperAdminContext`는 이미 존재 — 그대로 활용).

### 3.3 로그인 흐름 재설계

1. `authorize()`가 더 이상 `DEFAULT_PROJECT_ID`를 쓰지 않는다. 대신:
   - ID/비밀번호 검증 후, 해당 유저의 `isActive=true` `ProjectMember` 목록을 조회
   - **0개**: 로그인 거부("소속된 프로젝트가 없습니다. 가입 승인을 기다려 주세요.")
   - **1개**: 자동으로 그 프로젝트로 로그인 (선택 UI 노출 안 함)
   - **2개 이상**: 클라이언트가 프로젝트를 지정하지 않고 요청했다면 "PROJECT_REQUIRED" 오류를 반환 → 로그인 화면이 프로젝트 선택 UI를 노출 → 사용자가 고르면 `projectId`를 포함해 재요청
2. 로그인 화면: "프로젝트 저장(기억하기)" 체크 시 `User.lastProjectId`를 서버에 저장. 다음 로그인 때 후보가 2개 이상이면 `lastProjectId`를 기본 선택값으로 미리 채워준다(기기가 바뀌어도 유지되도록 로컬스토리지가 아닌 서버 저장을 권장).
3. 세션(JWT)의 `projectId`는 이렇게 확정된 프로젝트로 채워지며, 이후 로직(요구사항관리·WBS 등 모든 화면)은 **한 줄도 바꿀 필요 없이 지금처럼 `session.user.projectId`를 그대로 쓴다.**
4. (로드맵 3단계) 로그인 이후에도 프로젝트를 바꿀 수 있는 "프로젝트 전환" UI를 상단 메뉴에 추가 — 여러 프로젝트 소속 사용자를 위한 자연스러운 보완 기능.

### 3.4 회원가입 흐름 재설계

1. 가입 폼에 "프로젝트 검색" 입력을 추가 (프로젝트명/코드로 검색 → 자동완성 목록, 공개된 프로젝트만 노출).
2. 가입 제출 시 `User` 생성과 함께 해당 프로젝트에 `ProjectMember(role=MEMBER, isActive=false)`를 함께 생성 — **가입 승인 대기 상태**로 시작.
3. 프로젝트 PM/PMO에게 "가입 승인 관리" 화면(신규)에서 대기 목록을 보여주고 승인/반려 처리. 승인 시 `isActive=true`로 전환, 이 시점부터 로그인 가능.
   - 기존 "정기예약 승인"(`/settings/recurring-meetings`) 화면과 동일한 UX 패턴(대기 목록 → 승인/반려 버튼)을 재사용.
4. 한 사용자가 여러 프로젝트에 가입 신청하는 것은 제한하지 않는다(멀티 소속을 지원하는 것이 이번 플랫폼화의 취지이므로).

### 3.5 슈퍼관리자: 프로젝트 생성/관리 (신규 화면)

- 경로 제안: `/platform/projects` (슈퍼관리자 전용, 기존 `/settings/*`와 구분되는 플랫폼 레벨 네임스페이스)
- 프로젝트 생성 마법사:
  1. 기본정보 입력(기존 `Project` 필드: 코드/이름/고객사/벤더/오픈방식/일정 등 — 지금의 "프로젝트정보 설정" 폼과 동일 필드 재사용)
  2. PM 지정: 사용자 검색(이름/아이디) → 선택 → `ProjectMember(role=ADMIN)` 생성
  3. PMO 지정(복수 가능): 사용자 검색 → 선택 → `ProjectMember(role=OPERATOR)` 생성
  4. 상위 프로젝트 지정(선택): 기존 프로젝트 중 상위 프로젝트가 없는 것만 후보로 노출 → `parentProjectId` 설정
- 전체 프로젝트 목록 화면: 계층 구조(상위 프로젝트 아래 하위 목록 들여쓰기)로 표시, 프로젝트별 상태(멤버 수, 활성 기능 수) 요약.

### 3.6 프로젝트 PM: 정보 수정 및 기능 선택 (신규 화면)

- 기존 `/project-settings` 화면(PM/관리자 전용, 이미 존재)에 **"기능 선택" 탭**을 추가.
- `FEATURE_CATALOG`를 순회하며 각 항목을 토글 스위치로 표시. `tier: "base"`는 항상 켜진 상태로 비활성 표시(끌 수 없음), `tier: "addon"`만 실제 토글 가능.
- 기능을 끄면 해당 모듈은 `AppNavigation.tsx`의 메뉴 목록에서 제외되고, 관련 API 라우트도 `assertFeatureEnabled(projectId, key)` 가드로 차단(URL 직접 접근 방어).
- **확정 필요 사항**: 기본기능/추가기능 분류 기준(3.1절 후보안)은 실제 구현 착수 전 사용자 확인 필요.

### 3.7 상하위 프로젝트 및 통합 현황 (집계/롤업 보고)

- 슈퍼관리자만 `parentProjectId`를 지정/해제할 수 있다 (3.5절 화면에서 처리).
- 하위 프로젝트는 지금처럼 자신의 WBS·요구사항을 완전히 독립적으로 CRUD한다 — **데이터 소유권은 항상 개별 프로젝트에 남는다.**
- 상위 프로젝트 컨텍스트에서 신규 "통합 현황" 화면 진입 시:
  - 자기 자신 + 모든 하위 프로젝트의 WBS/요구사항 데이터를 프로젝트별로 묶어 집계
  - 기존 `RequirementStatisticsScreen`/`WBS 통계` 화면 패턴을 확장해 "프로젝트별 breakdown" 축을 추가(현재도 `요구사항분류×수용여부`, `업무 대분류별 분포` 같은 다차원 집계를 이미 하고 있어 동일한 패턴으로 자연스럽게 확장 가능)
  - **읽기 전용**이며, 항목을 클릭하면 해당 하위 프로젝트의 실제 상세 화면으로 드릴다운
- 접근 권한: 상위 프로젝트의 PM/PMO(또는 SUPER_ADMIN)만 통합 현황에 접근 가능. 하위 프로젝트 멤버가 상위 프로젝트 멤버가 아니라면 통합 뷰에는 접근할 수 없다(하위 프로젝트 데이터를 상위로 "승격"시키는 것이 아니라, 상위 프로젝트 담당자에게 "조회 권한"만 부여하는 개념).

### 3.8 결함관리 내부 모듈 통합

#### 3.8.1 통합 원칙

- 결함관리는 PMOTOOLS의 프로젝트 종속 **테스트 보조 추가기능(`featureKey: "defects"`)**으로 편입한다. 최종 기본/추가기능 분류는 3.6절의 기능 분류 확정 시 함께 승인한다.
- `defect_manage2` 소스를 `public/defects` 같은 정적 하위 폴더에 그대로 복사하지 않는다. 기존 앱은 별도 로그인, 루트 `/api/*`, `node_modules` 정적 참조, `localStorage` 세션에 의존하므로 PMOTOOLS와 충돌한다.
- 화면은 `/defects` 네임스페이스의 React/Next.js 화면으로, API는 `/api/v1/defects/*`로 이식한다.
- 인증은 NextAuth 세션을 사용하고 모든 조회·변경 쿼리는 `session.user.projectId`로 제한한다. 별도 결함관리 사용자·비밀번호 테이블은 만들지 않는다.
- 결함관리는 PMOTOOLS `items`(이슈관리)와 업무적 연관성이 없는 독립 도메인이다. 데이터 모델, 화면, API, 상태, 권한 규칙을 공유하거나 상호 연결하지 않는다.
- 결함에서 이슈로의 승격·전환, 양방향 링크, 상태 동기화 기능은 범위에 포함하지 않는다.

#### 3.8.2 기능 범위

- 테스트 수행 중 발견된 결함을 빠르게 수집하고 재현 정보·화면·환경·증적을 함께 기록하는 보조 도구
- 결함 대시보드: 상태·심각도·테스트 구분·조치완료율·조치 미완료 집계
- 결함 목록: 번호 정확검색, 조건 필터, 정렬, 페이지네이션, CSV/엑셀 다운로드
- 결함 등록·상세·수정·보관: 재현절차, 화면경로, 환경정보, 첨부 이미지, 담당자, 조치일정·결과
- 담당자 조치 현황 및 결함조치재확인
- 프로젝트별 결함 공통코드와 활성 테스트 구분 설정
- 저장 오류 로그와 결함 변경 이력
- 사용자 메뉴얼
- 테스트벤치·모바일 퀵 등록·Chrome 확장프로그램 연동

#### 3.8.3 권한

| 역할 | 결함관리 권한 |
|---|---|
| `SUPER_ADMIN` | 모든 프로젝트 결함 조회·관리, 설정 및 오류 로그 접근 |
| 프로젝트 `ADMIN`(PM) | 해당 프로젝트 전체 결함 관리, 공통코드·테스트 구분 설정, 데이터 내보내기 |
| 프로젝트 `OPERATOR`(PMO) | 해당 프로젝트 결함 등록·수정·조치·재확인, 담당 현황 조회 |
| 프로젝트 `MEMBER` | 해당 프로젝트 결함 조회·등록, 본인에게 허용된 수정·조치 |

- 실제 API 권한은 화면 노출과 별개로 서버에서 검증한다.
- `ProjectFeature(defects)`가 꺼진 프로젝트는 메뉴를 숨기고 `/defects` 및 `/api/v1/defects/*` 직접 접근도 차단한다.

#### 3.8.4 기존 데이터 이관

2026-09-04 로컬 `defect_manage2` 기준 이관 대상은 사용자 43명, 결함 141건(활성 139건), 공통코드 163건, 저장 오류 로그 21건, 기존 결함 이력 0건이다. 스크린샷은 117건, 문자열 기준 약 28MB이다. 실제 이관 직전에 다시 집계해 확정한다.

- 사용자: 이메일 또는 로그인 ID 기준으로 PMOTOOLS `User`와 매핑한다. 자동 매핑 실패·중복은 검토 목록으로 출력하며 임의 신규 계정을 만들지 않는다.
- 프로젝트: 이관 실행 시 대상 `projectId`를 필수 인자로 받는다. 모든 결함·코드·로그는 이 프로젝트에 귀속한다.
- 결함 번호: PMOTOOLS UUID와 별도로 `legacyDefectId`를 보존한다. 신규 표시번호는 프로젝트별 `DefectSequence`로 발급한다.
- 공통코드: PMOTOOLS `CommonCodeGroup`/`CommonCode` 구조에 맞춰 프로젝트별로 변환한다. 동일 코드 충돌 시 덮어쓰지 않고 변환 보고서에 남긴다.
- 담당자·작성자: User FK로 변환하되 매핑되지 않은 원본 문자열은 `legacyCreatorName`/`legacyAssigneeName`에 보존한다.
- 스크린샷: Base64 문자열을 DB에 그대로 넣지 않고 Supabase Storage의 프로젝트별 경로로 업로드한 후 경로/URL만 저장한다. 업로드 실패 항목은 재실행 가능한 manifest로 남긴다.
- 삭제 데이터: 물리 삭제하지 않고 `archivedAt`으로 변환한다.
- 검증: 테이블별 원본·성공·실패 건수, 원본 ID 중복, 필수값 누락, 사용자 미매핑, 이미지 체크섬 결과를 이관 보고서로 생성한다.

#### 3.8.5 외부 등록 연동

- 테스트벤치, 모바일 퀵 등록, Chrome 확장프로그램에서 사용하던 `pending_defect`/`postMessage` 데이터 규약은 초기 이관에서 호환 계층을 제공한다.
- 동일 도메인에서는 PMOTOOLS `/defects/new`로 이동해 세션 인증을 사용한다.
- 외부 도메인·확장프로그램 직접 등록은 일반 세션 또는 프로젝트별 제한 토큰 중 하나로 인증하고, 허용 origin·요청 크기·첨부파일 형식을 서버에서 검증한다.
- 기존 `defect_manage` 및 `defect_manage2` URL을 즉시 제거하지 않고 병행 운영 기간 동안 새 PMOTOOLS 경로 안내 또는 읽기 전용 전환을 제공한다.

#### 3.8.6 구현 구조

```text
app/defects/                         페이지(대시보드·목록·등록·상세·설정)
app/api/v1/defects/                  Route Handlers
features/defects/                    클라이언트 상호작용과 폼
screens/Defect*Screen.tsx            PMOTOOLS 공통 레이아웃 기반 화면
lib/domain/defects.ts                상태·심각도·검증·표시 규칙
lib/server/defects.ts                Prisma 조회·변경·통계
scripts/migrate-defect-manage2.ts    재실행 가능한 데이터 이관
public/manuals/defects.html          사용자 메뉴얼
```

`server.js`, 결함관리 전용 `users`, 독립 `common_codes`, 클라이언트 비밀번호 검증 코드는 이관 대상에서 제외한다.

## 4. 단계별 로드맵

### Phase 1 — 멀티 프로젝트 로그인/가입 기반 구축
데이터 모델과 인증 흐름을 다중 프로젝트 대응으로 전환. 이 단계가 끝나야 이후 모든 관리 화면이 의미를 가진다.

- [ ] `Project.parentProjectId`, `ProjectFeature`, `User.lastProjectId` 마이그레이션 추가
- [ ] `lib/server/auth.ts` 재작성 — `DEFAULT_PROJECT_ID` 하드코딩 제거, 멤버십 기반 로그인 + "PROJECT_REQUIRED" 흐름
- [ ] 로그인 화면: 프로젝트 선택 UI(2개 이상일 때만 노출) + "프로젝트 기억하기"
- [ ] 회원가입 화면: 프로젝트 검색·선택 UI 추가, `ProjectMember(isActive:false)` 동반 생성
- [ ] 가입 승인 관리 화면(PM/PMO용) 신규 추가
- [ ] 기존 `DEFAULT_PROJECT_ID` 참조 코드 전수 점검(스크립트, 시드, 문서) 및 정리

### Phase 2 — 플랫폼 관리 기능
슈퍼관리자와 PM에게 실제 운영 도구를 제공.

- [ ] 슈퍼관리자 "프로젝트 생성/관리" 화면(`/platform/projects`) — 기본정보 입력, PM/PMO 지정
- [ ] 상하위 프로젝트 지정 UI(슈퍼관리자)
- [ ] PM용 "기능 선택" 화면(`/project-settings` 확장) + `FEATURE_CATALOG` 정의 및 확정
- [ ] 메뉴/라우트에 기능 플래그 가드 적용(`AppNavigation.tsx`, API 라우트)

### Phase 3 — 통합 뷰 및 사용성 고도화
멀티 프로젝트 운영이 실제로 편해지는 단계.

- [ ] 상위 프로젝트용 "통합 WBS 현황" 화면
- [ ] 상위 프로젝트용 "통합 요구사항 현황" 화면
- [ ] 로그인 후에도 소속 프로젝트를 전환할 수 있는 "프로젝트 전환" UI(상단 메뉴)
- [ ] 기존 통계 화면에 프로젝트별 breakdown 열 추가

### Phase 4 — 결함관리 모듈 편입

플랫폼의 프로젝트 컨텍스트와 기능 플래그를 기반으로 기존 DefectFlow를 PMOTOOLS 내부 모듈로 이식한다.

- [ ] 테스트 보조 수단으로서 결함관리의 기능 범위와 역할별 권한 확정
- [ ] Prisma `Defect`/`DefectEvent`/`DefectErrorLog`/`DefectSequence` 모델과 마이그레이션 추가
- [ ] `FEATURE_CATALOG` 및 프로젝트 메뉴에 `defects` 추가, 화면·API 기능 가드 적용
- [ ] `/api/v1/defects/*` 서버 도메인과 Route Handler 구현
- [ ] `/defects` 대시보드·목록·등록·상세·조치현황·설정 화면 구현
- [ ] `defect_manage2` 사용자·공통코드·결함·오류 로그·스크린샷 이관 스크립트와 검증 보고서 구현
- [ ] 테스트벤치·모바일 등록·Chrome 확장프로그램의 PMOTOOLS 연동 주소와 인증 방식 전환
- [ ] 단위·통합·E2E 및 역할별·프로젝트별 데이터 격리 테스트
- [ ] 기존 서비스와 병행 검증 후 기존 DefectFlow를 읽기 전용으로 전환하고 최종 종료

## 5. 리스크 및 미해결 이슈

- **기존 운영 데이터 이관**: 지금의 `DEFAULT_PROJECT_ID` 프로젝트를 플랫폼 전환 후 "1번 프로젝트"로 그대로 유지해야 하며, 그 프로젝트에 속한 기존 사용자 전원의 `ProjectMember.isActive`가 `true`로 남아 있는지 마이그레이션 스크립트로 검증 필요.
- **기능 OFF 시 기존 데이터 처리 정책**: 어떤 프로젝트가 나중에 특정 기능(예: 이슈관리)을 끄면, 이미 쌓인 데이터를 숨기기만 할지(재활성화 시 복구), 접근 자체를 차단할지 정책 확정 필요.
- **PM 권한과 `ProjectMember.role=ADMIN` 재사용의 부작용**: 기존에 "관리자"라는 표현이 쓰이는 화면 문구들이 이제는 "PM"을 의미하게 되므로, 사용자 대상 UI 라벨을 프로젝트 컨텍스트에서는 "PM"으로 바꿔줄지 검토 필요.
- **통합 현황 성능**: 하위 프로젝트 수가 많아지면 집계 쿼리 비용이 커질 수 있음 — 초기엔 단순 집계로 시작하고, 필요 시 캐싱(기존에 쓰이는 `unstable_cache`/`revalidateTag` 패턴) 적용.
- **가입 승인 알림**: 승인 대기/승인 완료를 사용자에게 알릴 방법(기존 쪽지/초청함 기능 재사용 가능) 결정 필요.
- **기본기능/추가기능 분류표(3.6절)는 아직 확정안이 아님** — 실제 구현 착수 전 사용자 확인 필수.
- **테스트 보조 범위 이탈**: 결함관리가 일반 업무·리스크·이슈관리로 확장되지 않도록 테스트 구분, 재현절차, 증적, 조치와 재확인 기능에 범위를 한정해야 함.
- **결함 사용자 매핑**: `defect_manage2` 사용자와 PMOTOOLS 사용자가 일치하지 않거나 이메일이 중복될 수 있음 — 자동 생성·병합하지 않고 사전 매핑 보고서를 승인받은 뒤 이관.
- **스크린샷 이관**: 현재 DB 문자열로 저장된 이미지가 많아 DB 용량과 응답 크기 문제가 있음 — Storage 업로드, 체크섬 검증, 실패 manifest와 재실행 절차 필요.
- **외부 등록 보안**: 테스트벤치·확장프로그램의 기존 동일 출처/localStorage 전제가 PMOTOOLS 통합 후 달라짐 — CORS, 토큰 수명·권한, 프로젝트 지정 위조 방지를 설계해야 함.
- **기존 배포 계획 충돌**: `defect_manage2`를 기존 독립 `defect_manage` 서비스로 교체하는 계획은 PMOTOOLS 편입 계획과 양립하지 않음 — PMOTOOLS 병행 검증 완료 전까지 기존 서비스는 유지하고 최종 운영 대상을 별도로 승인해야 함.

## 6. 참고: 재사용 가능한 기존 자산

- 권한 체크: `lib/server/permissions.ts`(`assertManager`, `assertAdmin`, `isManagerRole`), `lib/server/context.ts`(`requireAdminContext`, `requireSuperAdminContext`)
- 설정 화면 표준 스택: `app/settings/common-codes/page.tsx` → `screens/CommonCodeSettingsScreen.tsx` → `app/api/v1/settings/common-code-groups/route.ts` → `lib/server/common-codes.ts`
- 승인 대기 UX 패턴: `/settings/recurring-meetings`
- 다차원 집계 화면 패턴: `screens/RequirementStatisticsScreen.tsx`, WBS 통계 화면
- 프로젝트별 데이터 격리: 거의 모든 `lib/server/*.ts` 도메인 함수가 이미 `projectId` 필터링을 하고 있음
- 결함관리 원본 기능: `D:\workspace\defect_manage2\js\modules`, `js\services\storage`, `server.js`, `local\schema.sql`
- 결함관리 외부 연동: `D:\workspace\defect_manage2\test_view`, `extension\defectflow-reporter`, `js\services\bridge-service.js`
- 결함관리 이관 기준 데이터: `D:\workspace\defect_manage2\local\postgres-data` (운영 이관 전 재집계 및 백업 필수)
