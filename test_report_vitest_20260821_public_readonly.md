# Vitest 테스트 결과 보고서 — 비로그인 공개 조회

## 테스트 결과

- 실행 명령: `npm.cmd run test`
- 결과: 성공
- 테스트 파일: 12개 통과
- 테스트 케이스: 65개 통과
- 실패: 0건
- 실행 시간: 4.21초
- 추가 정적 검증: `npm.cmd run lint` (`tsc --noEmit`) 통과

## 결함 사항 (Defects)

- 발견된 회귀 결함 없음.
- 기존 테스트 구성에는 미들웨어의 공개 GET/비공개 쓰기 메서드를 직접 실행하는 테스트가 없음.

## 결함 조치 사항

- 비로그인 사용자에게 `/calendar`, `/meetrooms`와 회의실 예약현황 GET API만 공개함.
- 캘린더의 등록·수정·검색·주요 이벤트 진입 및 회의실의 예약·수정·취소·정기예약 UI를 비로그인 상태에서 차단함.
- 회의실 POST/PATCH 등 쓰기 요청은 기존 인증 경계를 유지함.

## 모달 전환 추가 검증

- 로그인 화면 공개 조회를 모달 방식으로 전환한 후 Vitest 12개 파일 65건 통과.
- 최초 재실행에서 Vitest가 `tests-e2e`의 Playwright 파일을 수집하는 러너 충돌 1건 발견.
- `vitest.config.ts`에서 `tests-e2e/**`, `tests-jest/**`를 제외해 책임을 분리한 후 전체 재통과.
