"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function WeekStatusButton({id,status}:{id:string;status:"open"|"closed"}){const [pending,setPending]=useState(false),router=useRouter();async function toggle(){setPending(true);await fetch(`/api/v1/weeks/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:status==="open"?"closed":"open"})});setPending(false);router.refresh();}return <button className="button secondary" onClick={toggle} disabled={pending}>{pending?"처리 중…":status==="open"?"주차 마감":"마감 해제"}</button>}
