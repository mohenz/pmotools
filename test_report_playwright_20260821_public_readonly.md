# Playwright E2E 테스트 결과 보고서 — 비로그인 공개 조회

## 테스트 결과

- 실행 명령: `npm.cmd run test:e2e`
- 브라우저: Chromium
- 결과: 성공
- 테스트 시나리오: 4건 통과
- 실패: 0건
- 실행 시간: 47.9초
- 최종 빌드: `npm.cmd run build` 성공

검증한 사용자 흐름:

1. 로그인 화면에서 `캘린더 조회`, `회의실 예약현황 조회` 링크 노출
2. 비로그인 캘린더 진입 및 등록·검색·주요 이벤트 기능 비노출
3. 비로그인 회의실 예약현황 진입 및 내 예약·정기예약·예약 기능 비노출
4. 회의실 예약현황 GET 성공 및 비로그인 POST 401 차단

## 결함 사항 (Defects)

- 최초 실행은 시스템 `DATABASE_URL`이 중지된 로컬 PostgreSQL을 가리켜 `ECONNREFUSED`로 중단됨.
- 원격 PostgreSQL 재시도는 샌드박스 네트워크 제한으로 `EACCES`가 발생함.
- 애플리케이션 기능 결함 및 E2E 시나리오 결함은 발견되지 않음.

## 결함 조치 사항

- Playwright 웹 서버에 `VERCEL=1`을 지정해 `.env.local`의 `POSTGRES_PRISMA_URL`을 우선 사용하도록 테스트 실행 환경을 고정함.
- 네트워크 승인을 받아 원격 PostgreSQL 읽기 연결로 E2E를 재실행함.
- 테스트의 유일한 쓰기 요청은 빈 본문의 비로그인 POST이며 미들웨어에서 401로 차단되어 데이터 변경이 발생하지 않음을 확인함.

## 모달 전환 추가 검증

- 로그인 화면에서 캘린더 모달을 열고 iframe 내부 캘린더 제목이 표시되는지 검증함.
- 모달을 닫은 뒤 회의실 예약현황 모달을 열고 iframe 내부 화면이 표시되는지 검증함.
- `Project Management Tools`, `로그인 없이 조회`, 공개 화면의 중복 설명 문구가 제거됐는지 검증함.
- Chromium E2E 4건 전체 통과(28.7초).
- 캘린더 모달 최초 화면과 `다음` 기간 이동 후 로그인 링크 0건, 회의실 예약현황 모달 로그인 링크 0건을 추가 검증함. Chromium E2E 4건 전체 재통과(24.3초).
- 배포 전 콜드 서버에서 회의실 iframe이 기본 5초 대기를 초과하는 테스트 환경 결함을 발견함. 모달 테스트 제한 90초, 해당 iframe 표시 대기 30초로 명시한 뒤 사전검증 전체를 재실행했고 Chromium E2E 4건 전체 통과(56.5초).
