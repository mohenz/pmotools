import { getLocalContext } from "@/lib/server/context";import { listActivityLogs } from "@/lib/server/work-management";
export const dynamic="force-dynamic";
export default async function ActivityLogsPage(){const {projectId}=getLocalContext();const rows=await listActivityLogs(projectId);return <><header className="topbar"><div><h1>사용자 활동 내역</h1><p>주요 업무 데이터의 변경 이력을 조회합니다.</p></div></header><div className="content"><section className="panel"><div className="panel-head"><h2>최근 활동</h2><span>{rows.length}건</span></div><div className="table-wrap"><table><thead><tr><th>일시</th><th>사용자</th><th>행위</th><th>변경 요약</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td className="mono">{new Intl.DateTimeFormat("ko-KR",{dateStyle:"short",timeStyle:"short"}).format(new Date(r.createdAt))}</td><td>{r.actorName??"시스템"}</td><td><span className="badge">{r.action}</span></td><td className="title-cell">{r.afterData?Object.keys(r.afterData).slice(0,6).join(", "):"삭제"}</td></tr>)}</tbody></table></div></section></div></>}
