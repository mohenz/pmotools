import { NextRequest,NextResponse } from "next/server";import { getLocalContext } from "@/lib/server/context";import { updateCalendarEvent } from "@/lib/server/calendar";import { mutationErrorResponse } from "@/lib/server/http";
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const {projectId}=getLocalContext();try{return NextResponse.json({data:await updateCalendarEvent(projectId,(await params).id,await req.json())});}catch(e){return mutationErrorResponse(e);}}
